# name: Java CI with Gradle

# # 동작 조건 설정 : develop 브랜치에 push 혹은 pull request가 발생할 경우 동작한다.
# on:
#   push:
#     branches: [ "develop" ]
#   pull_request:
#     branches: [ "develop" ]

# permissions:
#   contents: read

# jobs:
#   # Spring Boot 애플리케이션을 빌드하여 도커허브에 푸시하는 과정
#   build-docker-image:
#     runs-on: ubuntu-latest

#     steps:
#     - uses: actions/checkout@v3

#     # 1. Java 17 세팅
#     - name: Set up JDK 17
#       uses: actions/setup-java@v3
#       with:
#         java-version: '17'
#         distribution: 'temurin'

#     - name: 디버깅 - 파일 생성 전 디렉터리 내용 확인
#       run: |
#         ls -R ./BackEnd/campusin

#     - name: make application.yml
#       working-directory: ./BackEnd/campusin
#       run: |
#         echo ls ./
#         mkdir -p ./src/main/resources/
#         echo "${{ secrets.APPLICATION_YML }}" | base64 --decode > ./src/main/resources/application.yml
#         find src

#     - name: 디버깅 - 파일 생성 후 디렉터리 내용 확인
#       run: |
#         ls -R ./BackEnd/campusin

#     # 2. Spring Boot 애플리케이션 빌드
#     - name: Build with Gradle
#       working-directory: ./BackEnd/campusin
#       run: |
#         chmod +x ./gradlew  # Gradle Wrapper에 실행 권한 부여
#         ./gradlew clean bootJar --refresh-dependencies --no-daemon

#     # 디버그 나중에 지우기
#     - name: debug dockerhub username
#       run: |
#         echo "USERNAME is: '${{ secrets.DOCKERHUB_USERNAME }}'"
#         echo "LENGTH is: '${{ secrets.DOCKERHUB_USERNAME }}' | wc -c"
        
#     # 3. Docker 이미지 빌드
#     - name: docker image build
#       working-directory: ./BackEnd/campusin
#       run: docker build --no-cache -t ${{ secrets.DOCKERHUB_USERNAME }}/github-actions-demo .

#     # 4. DockerHub 로그인
#     - name: docker login
#       uses: docker/login-action@v2
#       with:
#         username: ${{ secrets.DOCKERHUB_USERNAME }}
#         password: ${{ secrets.DOCKERHUB_PASSWORD }}

#     # 5. Docker Hub 이미지 푸시
#     - name: docker Hub push
#       working-directory: ./BackEnd/campusin
#       run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/github-actions-demo

#   # EC2에서 푸시된 이미지를 pull 받아 실행하는 과정
#   run-docker-image-on-ec2:
#     needs: build-docker-image
#     runs-on: self-hosted

#     steps:
#     # 0. DockerHub 로그인
#     - name: docker login
#       uses: docker/login-action@v2
#       with:
#         username: ${{ secrets.DOCKERHUB_USERNAME }}
#         password: ${{ secrets.DOCKERHUB_PASSWORD }}

#     # 1. 미사용 이미지를 정리
#     - name: delete old docker image
#       run: docker image prune -af

#     # 2. 최신 이미지 pull
#     - name: docker pull
#       run: docker pull ${{ secrets.DOCKERHUB_USERNAME }}/github-actions-demo

#     - name: docker debugger1
#       run: docker ps -a

#     # 3. 기존 컨테이너 중지
#     - name: docker stop container
#       run: docker stop $(sudo docker ps -q) 2>/dev/null || true

#     - name: docker debugger2
#       run: docker ps -a

#     # 4. Redis 컨테이너 실행 (기존 컨테이너가 있으면 중지 및 삭제 후 실행)
#     - name: run redis container
#       run: |
#         docker network create my_network || true  # 네트워크가 이미 있으면 무시
#         docker stop redis || true  # 기존 redis 컨테이너 중지
#         docker rm redis || true  # 기존 redis 컨테이너 삭제
#         docker run --name redis --network my_network -d -p 6379:6379 redis:latest


#     # 5. 새 Spring Boot 애플리케이션 컨테이너 실행
#     - name: run new Spring Boot container
#       run: |
#         docker run --name github-actions-demo --network my_network --rm -d \
#           -p 8080:8080 -p 9090:9090 \
#           ${{ secrets.DOCKERHUB_USERNAME }}/github-actions-demo


#     - name: docker debugger3
#       run: docker ps -a
